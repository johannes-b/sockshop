apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
    name: sockshop-production-carts
spec:
    hosts:
        - carts.production.35.225.255.188.xip.io
    gateways:
        - sockshop-production-gateway
    http:
        -
            route:
                -
                    destination:
                        host: carts.production.svc.cluster.local
                        subset: blue
                    weight: 100
                -
                    destination:
                        host: carts.production.svc.cluster.local
                        subset: green
                    weight: 0
